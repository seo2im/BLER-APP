import styled from 'styled-components/native'

const Background = styled.ImageBackground`
	width : 100%;
	height : 100%;
`

const View = styled.View`
	opacity : 1;

`

const Tier = styled.Image`

`

const Text = styled.Text`
	color : white;
`

export { Background, View, Tier, Text};