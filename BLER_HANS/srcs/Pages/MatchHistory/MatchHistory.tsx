import React from 'react'
import { Text } from 'react-native'

import { tUserGame } from '../../Modules/games/api'

const Component = ({ userGames } : { userGames : tUserGame[] }) => {
	const { 
		
	} = userGames;
	return (
		<>
			
		</>
	)
}

export default Component;